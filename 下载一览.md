# 下载一览

## macOS 第四版：视频提取器 3.0

本地发布目录：

`VideoExtractorMac/版本发布/第四版/安装包/`

包含文件：

- `视频提取器3.0.app`
- `视频提取器3.0.zip`
- `视频提取器3.0.dmg`

本版重点：

- YouTube 视频可选择 0、1、2 或 3 种字幕语言。
- 支持普通字幕和 YouTube 自动生成字幕。
- 字幕格式可选择 SRT、ASS，或两种都生成。
- 多语言字幕会合成到同一份外挂字幕中，方便外语学习。

验证：

- macOS Swift 源码类型检查通过。
- `.app` 本地 ad-hoc 签名验证通过。
- ZIP 完整性测试通过，且不包含 `__MACOSX` 辅助目录。
- DMG 镜像信息验证通过。

说明：

安装包属于发布产物，仓库 `.gitignore` 默认排除 `.app`、`.zip`、`.dmg` 和 `版本发布/` 目录。需要公开分发时，建议把安装包上传到 GitHub Releases。
