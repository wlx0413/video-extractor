# 视频提取器使用说明书

适用版本：macOS 版 2.1、Android 版 1.0、iOS 客户端  
更新日期：2026-05-24

## 1. 软件简介

“视频提取器”用于在用户有权保存、学习、备份或获得授权的前提下，分析公开视频链接，并下载视频、提取音频或保存图文图片。

软件支持输入单个或多个链接，分析后会显示标题、平台、作者、时长、缩略图、可用格式和下载队列状态。macOS 版内置 `yt-dlp` 与 `FFmpeg`，普通用户通常不需要额外安装命令行工具。Android 版会把完成的文件保存到系统媒体库。iOS 版采用“iPhone 客户端 + Mac 本地下载服务”的方式运行。

## 2. 合规使用说明

请只处理公开且你有权保存的内容。本软件不会、也不应被用于绕过 DRM、付费墙、会员限制、登录限制、私密视频限制或地区限制。

软件不会主动读取浏览器 cookies、登录信息、密码或 token。遇到需要登录、会员、授权或权限的链接时，软件会停止并显示错误提示。各平台规则会变化，平台支持属于尽力支持，某些链接可能会因平台限制而无法解析或下载。

## 3. 主要功能

- 支持每行一个链接，批量加入分析和下载队列。
- 自动识别 YouTube、Bilibili、小红书、抖音和未知平台。
- 支持最佳质量、1080p 或更低、720p 或更低、480p 或更低、自定义格式。
- 支持仅音频导出为 `m4a`、`mp3`、`wav`。
- 支持小红书图文内容的图片下载，前提是公开元数据中能解析到图片地址。
- 下载队列显示任务状态、进度、速度、剩余时间和输出路径。
- 支持暂停、继续、取消下载任务。
- 保存下载历史，并提供清理历史功能。
- 提供日志页面，便于排查分析失败、下载失败和工具状态问题。

## 4. macOS 版安装与启动

### 4.1 安装包位置

当前项目中可使用的 macOS 发行包位于：

```text
VideoExtractorMac/版本发布/第三版/安装包/视频提取器2.1.dmg
VideoExtractorMac/版本发布/第三版/安装包/视频提取器2.1.zip
```

推荐优先使用 `视频提取器2.1.dmg`。

### 4.2 安装步骤

1. 双击打开 `视频提取器2.1.dmg`。
2. 将“视频提取器2.1.app”拖入“应用程序”文件夹，或直接从解压目录运行。
3. 首次打开时，如果 macOS 安全提示阻止启动，可在“系统设置 > 隐私与安全性”中允许打开。
4. 打开 App 后，进入“下载”页面即可开始使用。

### 4.3 权限说明

macOS 版默认只访问 App 自己的工作目录，用于保存默认下载文件、历史记录和日志。用户在下载页或设置页主动选择保存目录后，App 才会访问该目录。

App 不请求管理员权限，不需要全盘访问，不读取浏览器 cookies、密码或 token。

## 5. macOS 版快速使用

### 5.1 下载视频

1. 打开“下载”页面。
2. 在“视频链接”输入框中粘贴链接。多个链接可每行输入一个。
3. 点击“分析链接”，或按 `Command + Return`。
4. 分析成功后，在“格式选择”区域查看标题、平台、作者、时长和可用格式。
5. 在“视频规格”中选择：
   - 最佳质量
   - 1080p 或更低
   - 720p 或更低
   - 480p 或更低
   - 自定义格式
6. 点击“下载视频”。
7. 在“下载队列”中查看进度、速度、剩余时间和输出路径。

### 5.2 提取音频

1. 先粘贴链接并点击“分析链接”。
2. 在“音频格式”中选择 `m4a`、`mp3` 或 `wav`。
3. 点击“仅音频”。
4. 等待下载和转换完成。

建议优先使用 `m4a`，通常速度快、体积较小；需要通用兼容时选择 `mp3`；需要未压缩音频时选择 `wav`。

### 5.3 下载小红书图片

1. 粘贴小红书公开图文链接。
2. 点击“分析链接”。
3. 如果软件解析到图片数量，会显示“下载图片”按钮。
4. 点击“下载图片”。

如果没有出现“下载图片”按钮，通常表示该链接不是图文内容，或公开元数据中没有可用图片地址。

### 5.4 批量处理链接

在输入框中每行放一个链接，然后点击“分析链接”。软件会逐个分析并加入队列。下载前可在队列中点击具体任务，确认选中的标题和格式后再下载。

## 6. macOS 版页面说明

### 6.1 下载页

下载页是主要工作区，包括链接输入、保存位置、格式选择和下载队列。

- “视频链接”：粘贴一个或多个公开链接。
- “保存位置”：显示当前默认保存目录，可点击“更改”选择新目录。
- “格式选择”：分析成功后显示媒体信息、视频规格、音频格式和可用格式。
- “下载队列”：显示任务状态、进度、速度、剩余时间、错误信息和输出路径。

### 6.2 历史页

历史页记录完成、失败或取消的任务。可点击“刷新”重新读取记录，也可点击“清理记录”清空历史。

清理历史只清理历史数据，不会删除已经下载的视频、音频或图片。旧的 `history.json` 会移动到“要删除的”目录保留。

### 6.3 设置页

设置页用于配置下载偏好、显示外观和命令行工具。

- 默认保存路径：设置下载文件默认保存位置。
- 默认视频质量：设置新任务默认使用的清晰度。
- 默认音频格式：设置音频提取默认格式。
- 下载完成后自动打开文件夹：任务完成后自动打开保存目录。
- 外观模式：跟随系统、浅色模式、深色模式。
- 命令行工具：检查或指定 `yt-dlp`、`FFmpeg` 路径。

默认情况下，macOS 版会优先使用 App 内置工具。只有在内置工具不可用或你想使用自己安装的版本时，才需要手动指定工具路径。

### 6.4 日志页

日志页显示最近运行日志。下载失败、分析失败、工具不可用、权限不足等问题，都可以先在这里查看原因。点击“刷新”可重新加载日志内容。

## 7. Android 版安装与使用

### 7.1 安装包位置

当前项目中 Android 安装包位于：

```text
视频提取器安卓版.apk
VideoExtractorAndroid/dist/VideoExtractorAndroid-1.0.apk
VideoExtractorAndroid/dist/VideoExtractorAndroid-1.0-aligned.apk
```

普通安装可优先使用根目录下的 `视频提取器安卓版.apk`。

### 7.2 系统要求

Android 版最低支持 Android 7.0（API 24），目标版本为 Android 15（API 35）。

### 7.3 安装步骤

1. 将 APK 发送到 Android 手机。
2. 在手机上打开 APK。
3. 如果系统提示“禁止安装未知来源应用”，请在系统设置中允许当前文件管理器或浏览器安装未知来源应用。
4. 安装完成后，打开“视频提取器”。

### 7.4 使用步骤

1. 打开 App，等待“下载核心初始化完成”或状态显示“就绪”。
2. 在“视频链接”输入框中粘贴公开链接。
3. 点击“分析链接”。
4. 分析成功后，选择视频规格或音频格式。
5. 点击“下载视频”“仅音频”或“下载图片”。
6. 在“下载队列”中查看进度。

### 7.5 Android 保存位置

Android 版完成后会自动保存到系统媒体库：

```text
视频：Movies/视频提取器
音频：Music/视频提取器
图片：Pictures/视频提取器
```

保存后通常可以在系统相册、文件管理器或音乐播放器中找到。不同手机厂商的媒体扫描速度不同，刚下载完成时可能需要等待片刻。

### 7.6 分享链接到 App

Android 版支持接收系统分享的文本链接。你可以在浏览器或其他 App 中使用“分享”，选择“视频提取器”，链接会自动填入输入框。

### 7.7 更新核心

Android 版日志区域提供“更新核心”按钮，用于更新下载核心。若遇到某些平台突然无法解析，可先尝试更新核心，再重新分析链接。

## 8. iOS 客户端使用说明

iOS 版不能像 macOS 版一样直接在手机内运行项目自带的 `yt-dlp` 和 `FFmpeg`，因此采用“iPhone App + Mac 本地下载服务”的架构。

### 8.1 使用前准备

你需要：

- 一台 Mac。
- 完整安装 Xcode。
- 一个 Apple ID。
- iPhone 开启 Developer Mode。
- iPhone 和 Mac 连接同一个 Wi-Fi。

### 8.2 启动 Mac 本地服务

在 `VideoExtractorMac` 目录下运行：

```bash
python3 VideoExtractorServer/server.py --host 0.0.0.0 --port 8765
```

查询 Mac 局域网 IP：

```bash
ipconfig getifaddr en0
```

然后在 iPhone App 顶部填写：

```text
http://你的Mac局域网IP:8765
```

模拟器可以使用：

```text
http://127.0.0.1:8765
```

### 8.3 iPhone 端操作

1. 确认下载服务显示在线。
2. 粘贴链接。
3. 点击分析。
4. 选择视频、音频或图片。
5. 查看进度并保存完成文件。

完成文件会保存到 iOS App 沙盒的 `Documents/Downloads` 目录，可通过系统分享面板导出到“文件”、AirDrop 或其他 App。

## 9. 格式与清晰度说明

“最佳质量”会尽量选择平台可提供的最佳视频流和音频流，并使用 FFmpeg 合并。清晰度选项如“1080p 或更低”表示优先选择不超过该清晰度的可用格式；如果平台没有对应规格，软件会选择可用的替代格式。

“自定义格式”适合高级用户。分析成功后，可从“可用格式”列表中选择具体格式 ID。某些平台的视频和音频是分离流，软件会尝试自动组合音频并合并。

## 10. 任务状态说明

- 分析中：正在读取链接元数据。
- 就绪：链接分析成功，可以开始下载。
- 下载中：正在下载文件。
- 转换中：下载完成后正在合并视频音频或转换音频格式。
- 已暂停：任务已暂停，可继续。
- 已完成：任务完成，文件已保存。
- 失败：任务失败，可查看错误提示或日志。
- 已取消：用户取消任务。

## 11. 常见问题

### 11.1 提示“当前链接暂不支持”

可能原因包括平台暂不支持、链接格式不正确、短链接跳转失败或平台规则变化。请确认链接能在浏览器中公开访问，并尝试更新下载核心。

### 11.2 提示“该链接需要登录或权限”

该内容可能是私密内容、会员内容、受地区限制内容或需要账号登录。本软件不会读取登录态，也不会绕过权限限制。

### 11.3 分析成功但下载失败

可能原因包括网络中断、平台临时限制、磁盘空间不足、保存目录不可写或 FFmpeg 不可用。macOS 版可进入“日志”页查看详细原因；Android 版可查看页面底部日志区域。

### 11.4 下载完成后找不到文件

macOS 版请查看“保存位置”显示的目录，或在历史页查看输出路径。Android 版请到 `Movies/视频提取器`、`Music/视频提取器`、`Pictures/视频提取器` 查找，并等待系统媒体库刷新。

### 11.5 小红书图片无法下载

只有当公开元数据中包含图片地址时，软件才能下载图片。若链接需要登录、内容不是图文、或平台返回的数据没有图片地址，软件会提示没有找到可下载图片。

### 11.6 下载速度很慢

下载速度取决于网络环境、平台限制、视频大小和当前服务器状态。可尝试切换网络、降低清晰度，或稍后重试。

### 11.7 macOS 提示工具不可用

进入“设置 > 命令行工具”，点击“检查工具”。如果内置工具不可用，可安装 Homebrew 版本并手动指定路径：

```text
/opt/homebrew/bin/yt-dlp
/opt/homebrew/bin/ffmpeg
```

## 12. 隐私与文件安全

软件只处理用户主动输入或分享进来的链接。macOS 版默认只写入 App 工作目录或用户手动选择的保存目录；Android 版保存到系统媒体库；iOS 版保存到 App 沙盒目录。

软件会清理下载文件名中的非法字符，避免路径冲突。历史清理和日志轮转不会直接删除旧文件，而是移动到“要删除的”目录保留。

## 13. 维护建议

如果某个平台突然无法解析，优先尝试：

1. 确认链接是公开可访问的 `http` 或 `https` 链接。
2. 更新 Android 版下载核心，或更新 macOS 版内置工具。
3. 降低视频清晰度后重试。
4. 查看日志中的错误信息。
5. 等待平台规则稳定后再试。

公开视频下载依赖平台公开页面和元数据，平台规则变化时，解析能力可能需要更新下载核心才能恢复。
