# yt-dlp

开发阶段可以把 `yt-dlp` 可执行文件放在这个目录：

```text
Tools/yt-dlp/yt-dlp
```

也可以在 App 的设置页选择本机已安装的 `yt-dlp` 路径，例如：

```text
/opt/homebrew/bin/yt-dlp
```

App 不会读取浏览器 cookies、密码或 token，也不会绕过登录、会员、DRM、地区或权限限制。
